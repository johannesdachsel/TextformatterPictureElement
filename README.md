# TextformatterPictureElement

Module for the fabulous [Processwire CMS](http://processwire.com) that replaces all `<img>` elements in a textarea with configurable `<picture>` elements.

The module allows setting multiple image sizes in combination with media queries and providing a fallback image size.